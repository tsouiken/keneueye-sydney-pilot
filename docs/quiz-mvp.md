# KenEyeCue 第一印象被低估測驗 MVP

> 狀態：可上線（模擬付款模式）｜最後更新：2026-08-16

## 這是什麼

陌生人從「看到貼文 → 私訊 Cue我 → 5 題測驗 → 出結果 → 解鎖 NT$499 完整報告」的入口網頁。

- 5 題測驗，作答完只顯示 Primary Result（例如「你目前最可能被低估的是：專業份量。」）
- 結果頁只有一個按鈕：**解鎖完整第一印象報告 NT$499**
- 付款完成後顯示成功頁，並說明後續步驟（LINE 聯絡、照片、7 題問卷、48 小時交付）

## 檔案結構

```
quiz/
  index.html      測驗 SPA（首頁 → 5 題 → 結果）
  checkout.html   NT$499 結帳頁
  success.html    付款成功頁
  quiz.css        品牌樣式（深炭黑 / 象牙白 / 暖金）
  quiz-logic.js   題目、計分、結果文案（瀏覽器與 Node 共用）
lib/ecpay.js      綠界 CheckMacValue 純函式（已用官方範例驗證）
server.js         零依賴 Node 伺服器（靜態檔 + 訂單 API）
test/             自動測試（計分、ECPay 簽章、API 流程）
```

## 5 種被誤讀的第一印象

| 維度 | 被低估的面向 | 結果文案開頭 |
|------|------------|------------|
| 太兇 | 親和力 | 你目前最可能被低估的是：親和力。 |
| 太累 | 狀態與精神 | 你目前最可能被低估的是：狀態與精神。 |
| 太軟 | 專業份量 | 你目前最可能被低估的是：專業份量。 |
| 太散 | 可靠度 | 你目前最可能被低估的是：可靠度。 |
| 太硬 | 溫度與彈性 | 你目前最可能被低估的是：溫度與彈性。 |

平手時依優先序：太軟 > 太散 > 太兇 > 太累 > 太硬。

## 付款（綠界 ECPay）

- 環境變數 `ECPAY_MERCHANT_ID` / `ECPAY_HASH_KEY` / `ECPAY_HASH_IV` 三個都填 → 真實金流
- 留空 → **模擬付款模式**（可完整測試流程，不會真的扣款）
- CheckMacValue 演算法已對齊綠界官方 PHP SDK，並用官方文件範例驗證通過
- 回傳驗證：簽章、金額（NT$499）、訂單存在性，全部通過才標記已付款

## 啟動

```bash
node server.js          # 預設 http://localhost:3000
npm test                # 跑全部測試
```

## 部署（Railway）

```bash
# 設定環境變數
PORT=3000
BASE_URL=https://<你的網域>          # 綠界回傳與付款完成跳轉用
ECPAY_MERCHANT_ID=                   # 留空 = 模擬付款
ECPAY_HASH_KEY=
ECPAY_HASH_IV=
```

## 上線前需要 Ken 提供的

1. **綠界商戶**：MerchantID / HashKey / HashIV（申請後填入即啟用真實收款）
2. **LINE 關鍵字回覆**：在 LINE 官方帳號後台設定「Cue我」→ 測驗網址（不用寫程式）
3. **付款後交付流程**：目前成功頁是「24 小時內 LINE 聯絡」的人工交接，若要自動化（7 題問卷、照片上傳、AI 報告）是下一步
